# Project C — TypeScript / Express

Equivalent Phase-8 benchmark target to Project A (Python/FastAPI) and Project B (Java/Spring Boot).

## Run

```bash
npm install
npm start
```

## Test and build

```bash
npm test
npm run build
```

## Intentional benchmark defect

`known_bug.md` documents the seeded defect used in later SPS-CA repair experiments. The equivalent defect exists in Projects A and B.
