# Project A — Python / FastAPI

Equivalent Phase-8 benchmark target to Project B (Java/Spring Boot) and Project C (TypeScript/Express).

## Run

```bash
python -m pip install -r requirements.txt
python main.py
```

## Test

```bash
pytest -q
```

## Intentional benchmark defect

`known_bug.md` documents the seeded defect used in later SPS-CA repair experiments. The equivalent defect exists in Projects B and C.
