# CAP-002 — Code Modification

Applies an explicit change to existing source code while preserving unrelated behavior.

**Allowed intent:** `code_modification`.

It requires existing source. It does not generate tests unless tests are explicitly requested as a separate intent.
