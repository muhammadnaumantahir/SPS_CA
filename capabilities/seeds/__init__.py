"""Seed capabilities CAP-001 through CAP-008.

Each subpackage contains ``metadata.json`` (CapabilityTemplate-shaped
metadata) and ``capability.py`` exposing a module-level ``run(context)``
function per the interface in ``capabilities.base``.
"""
