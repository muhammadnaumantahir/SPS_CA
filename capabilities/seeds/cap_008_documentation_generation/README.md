# CAP-008 — Documentation Generation

Finds undocumented Python functions/classes and can insert conservative docstring stubs derived from symbol names and function parameters.

Use `parameters['apply'] = true` to return updated source. The capability intentionally avoids inventing behavioral claims.